[pytest]
# pytest-django needs the settings module and the repo root on sys.path;
# without this config `pytest` collected nothing that touched Django.
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py tests.py
testpaths = tests appointments
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
