"""Service layer: use-case / business-logic modules.

Views (interface adapters) call into these services and translate their return
values and exceptions into HTTP responses. Services contain no HTTP concerns.
"""
