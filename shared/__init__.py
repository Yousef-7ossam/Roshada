"""Shared Streamlit frontend building blocks.

Extracted from the previously duplicated frontend scripts so the chat client,
history handling, reminders and backend API access exist in exactly one place
(DRY). Each app composes these helpers with its own page layout/CSS.
"""
